# Build context: project root (ap-tournament-app/)
FROM node:20-alpine

WORKDIR /app/backend

COPY backend/package*.json ./
RUN npm ci --omit=dev

COPY backend/src ./src

ENV NODE_ENV=production
ENV PORT=5001
ENV HOST=0.0.0.0

EXPOSE 5001

CMD ["node", "src/server.js"]
